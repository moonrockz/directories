///|
/// Package entry point for moonrockz/directories.
/// Public API: BaseDirs, UserDirs, ProjectDirs (see base_dirs.mbt, user_dirs.mbt, project_dirs.mbt).
/// OS detection: platform(), is_windows(), Platform enum (see platform.mbt).
